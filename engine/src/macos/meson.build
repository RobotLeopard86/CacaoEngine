# Frameworks
apple = dependency('appleframeworks', modules : ['Cocoa', 'IOKit', 'CoreFoundation', 'GameController'])

# macOS module
engine_srcs += files('MacOSTypes.hpp', 'MacOSWindow.mm')
engine_inc += include_directories('.')
engine_deps += apple